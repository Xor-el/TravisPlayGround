# Part of `travis-lazarus` (https://github.com/nielsAD/travis-lazarus)
# License: MIT

language: generic
sudo: required
dist: trusty

os:
  - linux
  # - osx

env:
  global:
    - WINEPREFIX=~/.winelaz
    - DISPLAY=:99.0
    - OSX_DEPLOY="false" LINUX_DEPLOY="false" WINDOWS_DEPLOY="false"
    - LAZ_ENV=
  matrix:
    - LAZ_VER=1.8.4 # Use specific (binary) release

matrix:
  include:
    # - os: linux
    #   env: LAZ_VER=1.8.4 LAZ_ENV=wine WINEARCH=win32 LAZ_OPT="--os=win32 --cpu=i386"
    # - os: linux
    #   env: LAZ_VER=1.8.4 LAZ_ENV=wine WINEARCH=win64 LAZ_OPT="--os=win64 --cpu=x86_64"
    # - os: linux
    #   env: LAZ_VER=1.8.4 LAZ_ENV=qemu-arm LAZ_OPT="--os=linux --cpu=arm"

before_install:
  # Start virtual display server
  - Xvfb $DISPLAY &
  - if [ "$TRAVIS_OS_NAME" == "linux" ]; then
          sudo apt-get update;
          sudo apt-get install binutils-2.26;
          sudo ln -sf /usr/lib/binutils-2.26/bin/* /usr/bin/;
          sudo ln -sf /usr/lib/binutils-2.26/ldscripts/* /usr/lib/ldscripts/;
    fi
  - chmod +x .travis.install.py 

install:
  # Install prerequisites (fpc/lazarus/wine/qemu)
  - ./.travis.install.py

script:
  - lazbuild $LAZ_OPT ./src/travisplayground.lpi  # Build project
  - $LAZ_ENV ./src/bin/travisplayground # Run binary
  # - sha1sum ./src/bin/travisplayground > ./src/bin/travisplayground.sha1

before_deploy:
# - if [[ ("$TRAVIS_OS_NAME" == "osx") && ("${TRAVIS_TAG}" =~ ^[0-9]+\.[0-9]+\.[0-9]+\.osx$) ]]; then export OSX_DEPLOY="true"; fi
# - if [[ ("$TRAVIS_OS_NAME" == "linux") && ("${TRAVIS_TAG}" =~ ^[0-9]+\.[0-9]+\.[0-9]+\.linux$) ]]; then export LINUX_DEPLOY="true"; fi
# - if [[ ("$LAZ_ENV" == "wine") && ("${TRAVIS_TAG}" =~ ^[0-9]+\.[0-9]+\.[0-9]+\.windows$) ]]; then export WINDOWS_DEPLOY="true"; fi

deploy:
  # # OS X deploy
  # provider: releases
  # api_key: '$GITHUB_API_KEY'
  # overwrite: true
  # file: 
  #   - ./src/bin/travisplayground
  # skip_cleanup: true
  # name: "travisplayground_OSX"
  # on:
  #   branch: master
  #   condition: $OSX_DEPLOY = "true"
  #   repo: Xor-el/TravisPlayGround
  #   tags: true

  # Linux deploy
  provider: releases
  api_key: '$GITHUB_API_KEY'
  overwrite: true
  file: 
    - ./src/bin/travisplayground
  skip_cleanup: true
  # name: "travisplayground_LINUX"
  on:
    # branch: master
    # condition: $LINUX_DEPLOY = "true"
    condition: ($TRAVIS_OS_NAME == "linux") && ($TRAVIS_TAG =~ ^[0-9]+\.[0-9]+\.[0-9]+\.linux$)
    repo: Xor-el/TravisPlayGround
    tags: true

  # Windows deploy
  # provider: releases
  # api_key: '$GITHUB_API_KEY'
  # overwrite: true
  # file: 
  #   - ./src/bin/travisplayground
  # skip_cleanup: true
  # name: "travisplayground_WINDOWS.exe"
  # on:
  #   # branch: master
  #   condition: $WINDOWS_DEPLOY = "true"
  #   repo: Xor-el/TravisPlayGround
  #   tags: true

notifications:
  email:
    on_success: false
    on_failure: change
