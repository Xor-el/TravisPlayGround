# Part of `travis-lazarus` (https://github.com/nielsAD/travis-lazarus)
# License: MIT

language: generic
sudo: required
dist: trusty

os:
  - linux
  - osx

env:
  global:
    - WINEPREFIX=~/.winelaz
    - DISPLAY=:99.0
    - OSX_DEPLOY="false" LINUX_DEPLOY="false" WINDOWS_DEPLOY="false"
    - LAZ_ENV=
  matrix:
    - LAZ_VER=1.8.4 # Use specific (binary) release

matrix:
  include:
    # - os: linux
    #   env: LAZ_VER=1.8.4 LAZ_ENV=wine WINEARCH=win32 LAZ_OPT="--os=win32 --cpu=i386"
    - os: linux
      env: LAZ_VER=1.8.4 LAZ_ENV=wine WINEARCH=win64 LAZ_OPT="--os=win64 --cpu=x86_64"
    # - os: linux
    #   env: LAZ_VER=1.8.4 LAZ_ENV=qemu-arm LAZ_OPT="--os=linux --cpu=arm"

before_install:
  # Start virtual display server
  - Xvfb $DISPLAY &
  - if [ "$TRAVIS_OS_NAME" == "linux" ]; then
          sudo apt-get update;
          sudo apt-get install binutils-2.26;
          sudo ln -sf /usr/lib/binutils-2.26/bin/* /usr/bin/;
          sudo ln -sf /usr/lib/binutils-2.26/ldscripts/* /usr/lib/ldscripts/;
    fi
  - chmod +x .travis.install.py 

install:
  # Install prerequisites (fpc/lazarus/wine/qemu)
  - ./.travis.install.py

script:
  - lazbuild $LAZ_OPT ./src/travisplayground.lpi  # Build project
  - $LAZ_ENV ./src/bin/travisplayground # Run binary
  # - sha1sum ./src/bin/travisplayground > ./src/bin/travisplayground.sha1

# deploy:
#   # OS X deploy
#   provider: releases
#   api_key: '$GITHUB_API_KEY'
#   overwrite: true
#   file: 
#     - ./src/bin/travisplayground
#   skip_cleanup: true
#   name: $TRAVIS_TAG
#   on:
#     branch: master
#     condition: ($TRAVIS_OS_NAME == "osx") && ($TRAVIS_TAG =~ ^v[0-9]+\.[0-9]+\.[0-9]+\_osx$)
#     repo: Xor-el/TravisPlayGround
#     tags: true

#   # Linux deploy
#   provider: releases
#   api_key: '$GITHUB_API_KEY'
#   overwrite: true
#   file: 
#     - ./src/bin/travisplayground
#   skip_cleanup: true
#   name: $TRAVIS_TAG
#   on:
#     condition: ($TRAVIS_OS_NAME == "linux") && ($TRAVIS_TAG =~ ^v[0-9]+\.[0-9]+\.[0-9]+\_linux$)
#     repo: Xor-el/TravisPlayGround
#     tags: true

  # Windows deploy
  provider: releases
  api_key: '$GITHUB_API_KEY'
  overwrite: true
  file: 
    - ./src/bin/travisplayground
  skip_cleanup: true
  name: $TRAVIS_TAG
  prerelease: true
  body: "Test Body"
  on:
    condition: ($LAZ_ENV == "wine") && ($TRAVIS_TAG =~ ^v[0-9]+\.[0-9]+\.[0-9]+\_windows$)
    repo: Xor-el/TravisPlayGround
    tags: true

notifications:
  email:
    on_success: false
    on_failure: change
